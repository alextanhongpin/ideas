# RAG

Create a sample RAG that can process all the common document types, e.g. text, markdown, image, pdfs.

Allow creating grounded responses, similar to NotebookLLM.

## Text

Store as plain text in postgres, then perform initial similarity search before chunking?

## PDF

Needs to be converted to images first.

## Image

Can be interpreted by VL models

## Eval

The most important is the evaluation
